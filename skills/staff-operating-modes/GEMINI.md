# GEMINI.md — Staff Operating Modes

> For Gemini CLI. The full authoritative reference is imported below; apply it whenever the task is in
> this skill's domain (see SKILL.md for triggers). This is a process/workflow skill — pick the mode,
> write the goal + the distinguished standard-of-done first, validate the real end-to-end path after each
> step, and pair every mode with a verification + review gate. Match the surrounding conventions.

@./staff-operating-modes-guide.md
